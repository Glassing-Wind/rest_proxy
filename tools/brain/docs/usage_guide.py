"""Former GraphRAG usage-guide tool registration."""

from mcp.server.fastmcp import FastMCP


def register(mcp: FastMCP) -> None:
    """The self-referential guide tool was pruned from the MCP surface."""
    return None
